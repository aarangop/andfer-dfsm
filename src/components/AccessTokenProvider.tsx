"use client";

export default function AccessTokenProvider() {
  // // const session =  fetch("http://localhost:3000/api/auth/accessTokens");
  // // console.log(session);
  // // if (session) {
  // // }
  // useEffect(() => {
  //   fetch("http://localhost:3000/api/auth/accessTokens")
  //     .then((res) => res.json())
  //     .then((data) => {
  //       if (data.accessToken) {
  //         store.dispatch(setAccessToken(data.accessToken));
  //       }
  //     });
  // }, []);
  return null;
}
